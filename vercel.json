{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build"
        }
    ],
    "buildCommand": "npm install --force && npm run build",
    "outputDirectory": "dist"
}